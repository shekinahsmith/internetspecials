<?
	/*
	 * this include is intended for additional assets that need to be loaded in the header SPECIFICALLY.
	 *
	 * if it is something JS related, more than likely you can split includes/{theme}/base/_footer-assets-additional.php and add it there,
	 * unless you need it it load before the DOM renders for some reason.
	 */
?>

